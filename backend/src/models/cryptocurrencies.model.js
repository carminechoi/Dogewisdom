import { Pool } from 'pg';
import db from '../models';

async function getCryptocurrency(id) {
    // const text = 'SELECT * FROM cryptocurrencies WHERE id = $1';
    // const values = [id];
    // return db.query(text, values);
}